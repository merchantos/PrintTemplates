---
extends: default

ignore: |
  node_modules/
  vendor/
  charts/
  docker-compose*.yml
  docker-compose*.yaml

rules:
  line-length:
    max: 120
    level: warning
    ignore:
      - .circleci/*.yml
  braces:
    min-spaces-inside: 0
    max-spaces-inside: 2
